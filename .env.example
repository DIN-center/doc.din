# GitHub personal access token with repo read access.
# Required to fetch SDK guide content from the private DIN-center/din-sdk-ts repo.
# Generate at: https://github.com/settings/tokens (classic, repo scope)
# Or use: gh auth status -t
GITHUB_TOKEN=
